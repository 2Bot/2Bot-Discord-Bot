package commands

import "github.com/AsynkronIT/protoactor-go/actor"

type BigEmojiActor struct{}

func (e *BigEmojiActor) Receive(context actor.Context) {
	/* msg := context.Message().(BigEmojiMsg)
	context. */
}
